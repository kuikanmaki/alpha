<h5><%= @page.name %></h5>

<p><span class="glyphicon icon-arrow-right"></span> <%= link_to "Page", page_path(@page) %></p><hr>

<% if @notes.any? %>
  <ol class="notes">
    <%= render @notes %>
  </ol>
    <%= will_paginate @notes %>
<% else %>
  <div class="emptypage">
    | 
  </div>
<% end %>